export function getFreshStock(ticker) {
  
}

const stockObjKeys = [
  "lastRefreshed", "date", "ticker", "open", "high", "low", "close", "volume", "ex-dividend", "split_ratio", "adj_open", "adj_high", "adj_low", "adj_close", "adj_volume"
]

export function validateStock(stockObj) {
  // TODO
}

export function isStockStale(stock) {
  
}